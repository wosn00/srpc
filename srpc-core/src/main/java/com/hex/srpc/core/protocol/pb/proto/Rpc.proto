syntax = "proto3";

package com.hex.netty.protocol.pb.proto;

message Packet {

    // 序列
    int64 seq = 1;

    // packet头部信息，可用于扩展
    string header = 2;

    // 指令，用于服务端分发请求，类似http url
    string cmd = 3;

    // 指令类型，0=心跳，1=请求，2=响应
    int32 commandType = 4;

    // 结果码，作为response时才具备
    int32 code = 5;

    // 时间戳
    int64 ts = 6;

    // 内容
    string body = 7;
}

message Err {

    int32 code = 1;

    string key = 2;
}